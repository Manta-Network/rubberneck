/*
const apiBaseUrl = (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1')
  ? `http://localhost:3001/prod`
  : `https://5eklk8knsd.execute-api.eu-central-1.amazonaws.com/prod`;
*/
const apiBaseUrl = `https://5eklk8knsd.execute-api.eu-central-1.amazonaws.com/prod`;
export default apiBaseUrl;
